apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "queryflux.fullname" . }}-test-connection"
  labels:
    {{- include "queryflux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command:
        - wget
      args:
        - -qO-
        - "http://{{ include "queryflux.fullname" . }}:{{ .Values.service.ports.admin.port }}/health"
