# ─── PRODUCTION CHECKLIST ─────────────────────────────────────────────────────
# The defaults below are tuned for a quick local evaluation, NOT for production.
# Before going live, review and change at minimum:
#
#   1. replicaCount       → ≥ 2 for HA
#   2. adminCredentials   → set a strong password (or use existingSecret)
#   3. persistence.type   → postgres (inMemory loses state on restart)
#   4. auth.provider      → oidc / ldap / static (none = unauthenticated)
#   5. auth.required      → true
#   6. resources          → set CPU/memory requests and limits
#   7. ingress / TLS      → enable HTTPS termination
#   8. config.existingSecret → use when config contains credentials
#
# Starter: examples/production-values.yaml + examples/production-config.yaml
# ──────────────────────────────────────────────────────────────────────────────

replicaCount: 1

image:
  repository: ghcr.io/lakeops-org/queryflux
  pullPolicy: IfNotPresent
  # Overrides the image tag. Defaults to the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

args:
  - --config
  - /etc/queryflux/config.yaml

serviceAccount:
  create: true
  automount: false
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000

adminCredentials:
  # WARNING: Change these for production! Default admin/admin is insecure.
  username: admin
  # Leave empty to generate a release-local password in a Secret.
  password: ""

existingSecret:
  name: ""
  usernameKey: QUERYFLUX_ADMIN_USER
  passwordKey: QUERYFLUX_ADMIN_PASSWORD

config:
  create: true
  # Mount config.yaml from a pre-created ConfigMap (set create: false).
  existingConfigMap: ""
  # Mount config.yaml from a pre-created Secret instead of a ConfigMap. Use this
  # when the config contains sensitive values (e.g. a Postgres URL with a
  # password), since the rendered config is mounted verbatim and QueryFlux does
  # not interpolate environment variables into the config file. Takes precedence
  # over existingConfigMap and config.create.
  existingSecret: ""
  mountPath: /etc/queryflux
  fileName: config.yaml
  data:
    queryflux:
      externalAddress: 'http://{{ include "queryflux.fullname" . }}:8080'
      frontends:
        trinoHttp:
          enabled: true
          port: 8080
      persistence:
        # WARNING: inMemory loses all state on pod restart. Use 'postgres' for production.
        type: inMemory
      adminApi:
        port: 9000
    clusterGroups:
      duckdb-local:
        engine: duckDb
        maxRunningQueries: 4
        clusters:
          - name: duckdb-1
    translation:
      errorOnUnsupported: false
    routers:
      - type: protocolBased
        trinoHttp: duckdb-local
    routingFallback: duckdb-local

service:
  type: ClusterIP
  annotations: {}
  ports:
    trinoHttp:
      enabled: true
      name: trino-http
      port: 8080
      targetPort: 8080
      protocol: TCP
    admin:
      enabled: true
      name: admin
      port: 9000
      targetPort: 9000
      protocol: TCP
    studio:
      enabled: true
      name: studio
      port: 3000
      targetPort: 3000
      protocol: TCP
    postgresWire:
      enabled: false
      name: postgres-wire
      port: 5432
      targetPort: 5432
      protocol: TCP
    mysqlWire:
      enabled: false
      name: mysql-wire
      port: 3306
      targetPort: 3306
      protocol: TCP
    snowflakeHttp:
      enabled: false
      name: snowflake-http
      port: 8443
      targetPort: 8443
      protocol: TCP
    flightSql:
      enabled: false
      name: flight-sql
      port: 32010
      targetPort: 32010
      protocol: TCP

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: queryflux.local
      paths:
        - path: /
          pathType: Prefix
          servicePortName: trino-http
  tls: []

# WARNING: Resource requests below are minimal for local evaluation.
# For production, set requests ≥ 500m CPU / 512Mi memory and appropriate limits.
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 1Gi

livenessProbe:
  httpGet:
    path: /health
    port: admin
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 2
  failureThreshold: 6

# Delays liveness/readiness until the process is ready (e.g. slow startup with
# large configs). Default allows up to ~5 minutes (periodSeconds * failureThreshold).
startupProbe:
  httpGet:
    path: /readyz
    port: admin
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 60

readinessProbe:
  httpGet:
    path: /readyz
    port: admin
  initialDelaySeconds: 3
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 6

# Must be ≥ queryflux.shutdownDrainTimeoutSecs (default 30) plus a buffer so
# Kubernetes does not SIGKILL mid-drain.
terminationGracePeriodSeconds: 45

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: null

pdb:
  enabled: false
  minAvailable: null
  maxUnavailable: 1

networkPolicy:
  # Keep false unless you supply non-empty ingress/egress for every declared
  # policyType. Empty rules + enabled = deny-all; the chart fails install then.
  # See examples/networkpolicy-values.yaml.
  enabled: false
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []

serviceMonitor:
  enabled: false
  namespace: ""
  labels: {}
  annotations: {}
  interval: 30s
  scrapeTimeout: 10s
  path: /metrics
  port: admin

env: []
envFrom: []
extraVolumes: []
extraVolumeMounts: []
extraContainers: []
lifecycle: {}

nodeSelector: {}
tolerations: []
affinity: {}
topologySpreadConstraints: []
